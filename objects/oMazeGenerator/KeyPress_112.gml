// KeyPress Event: F1 key (key code 112)
// This event is triggered when the player presses the F1 key.
// It restarts the game, which will generate a new random maze.

// Restart the game
// This will reload the room, causing the Create event to run again,
// which will generate a completely new maze with different layout.
game_restart();